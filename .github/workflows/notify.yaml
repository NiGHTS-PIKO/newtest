name: Pi Debug Test

on:
  push:
    branches:
      - master

jobs:
  debug-runner:
    runs-on: self-hosted
    steps:
      - name: Check Pi Runner health
        run: echo "🎉 Pi runner is functioning and received the job!"
