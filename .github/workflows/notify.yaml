name: Pi Minimal Test

on:
  push:
    branches:
      - master

jobs:
  webhook:
    runs-on: [self-hosted, ARM64, Linux]
    steps:
      - name: Test Echo
        run: echo "🎉 GitHub Runner is alive!"